import pytest
# Archived script: exercised the background mark worker end-to-end; the worker was removed in favor of explicit TUI scraping. See docs/ARCHIVED_TESTS.md
pytest.skip("mark worker removed; test archived — see docs/ARCHIVED_TESTS.md", allow_module_level=True)
