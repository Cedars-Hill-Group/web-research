import pytest
# Archived test: verified Flask mark endpoint and queue routing. Feature removed; see docs/ARCHIVED_TESTS.md
pytest.skip("mark worker removed; test archived — see docs/ARCHIVED_TESTS.md", allow_module_level=True)