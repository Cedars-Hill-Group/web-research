import pytest
# Archived test: verified hotkey persistence across SPA navigation. Feature removed; see docs/ARCHIVED_TESTS.md
pytest.skip("marking UI removed; test archived — see docs/ARCHIVED_TESTS.md", allow_module_level=True)
